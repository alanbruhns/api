[tool.poetry]
name = "api"
version = "0.1.0"
description = "Api"
authors = ["Alan Bruhns <alanbruhns@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
Flask = "^3.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
